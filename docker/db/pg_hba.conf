# TYPE  DATABASE        USER            ADDRESS                 METHOD

# Локальные подключения (уже есть)
local   all             all                                     trust

# Подключения по TCP/IP от любой сети Docker (самое важное)
host    all             all             172.20.0.0/16           md5
host    all             all             0.0.0.0/0               md5

# IPv6 (если нужно)
host    all             all             ::/0                    md5
